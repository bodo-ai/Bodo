/**
 * Parse tree for additional SQL DML statements.
 *
 * These are available in the extended SQL parser that is part of Bodo calcite module; the core parser in the
 * "core" module only supports SELECT and standard DML.
 */

@PackageMarker package com.bodosql.calcite.sql.dml;

import org.apache.calcite.avatica.util.PackageMarker;

// End package-info.java
