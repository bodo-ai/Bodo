trigger:
- refs/tags/*

pr: none

schedules:
- cron: "0 5 * * MON-FRI" 
  displayName: 'Weekdays Daily Midnight Build'
  branches:
    include:
    - develop

stages:
- stage: Build_Iceberg_Connector_Binary
  displayName: 'Build Iceberg Connector Binary'
  jobs:
  - template: buildscripts/iceberg/azure-build-binary.yml
    parameters:
      name: Noarch
      vmImage: ubuntu-latest
      pythonVersion: '3.8'
